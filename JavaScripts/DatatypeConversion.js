// now we are the learning the data type conversion 
let score  = 33 ; 
console.log(typeof(score)) ; 
// now check for the string 
let SecondScore = "33" ; 
//again check for the type 
console.log(typeof(SecondScore)) ; 

// now check for all the possible now i want to the operation in the number so we need to change to the string in the number 
let SecondScore_number = Number(SecondScore) ; 
console.log(typeof(SecondScore_number)) ;

// check the different  case 

let SecondScore_1 = "343abc" ; 
// now convert in the number 
let SecondScore_number_1 = Number(SecondScore_1) ;  // found not error in the conversion 
// now check the type 
console.log(typeof(SecondScore_number_1)) 
console.log(SecondScore_number_1) // Nan


// now check the different case 

let SecondScore_2 = null ; 
// now convert in the number 
let SecondScore_number_2 = Number(SecondScore_2) ;  // found not error in the conversion 
// now check the type 
console.log(typeof(SecondScore_number_2)) 
console.log(SecondScore_number_2) // 0

//check the different case 
let SecondScore_3 = undefined ; 
// now convert in the number 
let SecondScore_number_3 = Number(SecondScore_3) ;  // found not error in the conversion 
// now check the type 
console.log(typeof(SecondScore_number_3)) 
console.log(SecondScore_number_3) // Nan

// now check the different  the values or case 

let SecondScore_4 = true  ;  // check for the true and false 
// now convert in the number 
let SecondScore_number_4 = Number(SecondScore_4) ;  // found not error in the conversion 
// now check the type 
console.log(typeof(SecondScore_number_4)) 
console.log(SecondScore_number_4) // Nan


// now check the different  the values or case 

let SecondScore_5 = "shviam singh "  ;  // check for the true and false 
// now convert in the number 
let SecondScore_number_5 = Number(SecondScore_5) ;  // found not error in the conversion 
// now check the type 
console.log(typeof(SecondScore_number_5)) 
console.log(SecondScore_number_5) // Nan not a n number 


// hear we check for the number convert all the case in the number 
// convert all the datatype with the with the different different datatype with the all the case 

// create the table how acctuly  converting
// example 

// "shviam singh " => NaN
// "33" => 33
// "33abc" => NaN
// null => 0 


// now convert the boolean in the number 

let boll = true ; 
console.log(Number(boll)) ; // write the ans // write all the different case 


// convert in the string 

let someNumber = 44 ; 
console.log(String(someNumber))  
// check the type 
console.log(typeof(someNumber)) ;  // so actually convert 


// check the convert in in the object , arrays ........ with tha all the possible case 


